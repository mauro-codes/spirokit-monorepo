import { Source, ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs"
import dedent from "ts-dedent"
import { Box, VStack, Text, LargeText, Button } from "spirokit"

<Meta 
	title="Getting started/Typography/Loading a custom font"
    component={Text}  
	parameters={{
		viewMode: 'docs',
		previewTabs: { 
			canvas: { hidden: true } 
		},
	}}
/>

# Loading a custom font

TODO - Example for Expo projects

TODO - Example for React Native projects

- Include:
    - Install library
    - Load fonts with a custom strategy
    - Setup SpiroKitProvider