import { Source, ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs"
import dedent from "ts-dedent"
import { Box, VStack } from "spirokit"
import { 
	groupArgTypes,
	groupParameters,
	GroupArgumentsTableComponent,
	valueAndDefaultValueParameters,
	ValueAndDefaultValueComponent,
	sizeParameters,
	SizeComponent,
	spacingParameters,
	SpacingComponent,
	accentColorParameters,
	AccentColorComponent,
	colorModeParameters,
	ColorModeComponent,
	accentColorAndColorModeParameters,
	AccentColorAndColorModeComponent,
} from "./group-examples"

import { spaceArgTypes, SpaceArgTypesComments } from "../../arg-types/StyleProps"

<Meta 
	title="Components/Radio button/Group"  
	parameters={{
		viewMode: 'docs',
		previewTabs: { 
			canvas: { hidden: true } 
		},
	}}
/>

# Radio

## Basic example

<Canvas withSource="open">
	<Story
		name="Basic example"
		parameters={groupParameters}>
		<GroupArgumentsTableComponent></GroupArgumentsTableComponent>
	</Story>
</Canvas>

## Value & Default value

You can specify an initial value for the radio group using the `value` prop. This is specially useful to pre-populate forms.
You can also use the `defaultValue` prop to set a default value when `value` is not specified.

<Canvas withSource="open">
	<Story
		name="Value & Default value"
		parameters={valueAndDefaultValueParameters}>
		{(args) => {
			return (
				<ValueAndDefaultValueComponent></ValueAndDefaultValueComponent>
			)
		}}
	</Story>
</Canvas>

## Size

You can choose between `sm`, `md` and `lg` sizes for the radio buttons. This size will be propagated to each `Radio` component inside the group.

<Canvas withSource="open">
	<Story
		name="Size"
		parameters={sizeParameters}>
		{(args) => {
			return (
				<SizeComponent></SizeComponent>
			)
		}}
	</Story>
</Canvas>

## Spacing

You can optionally specify the vertical spacing between each `Radio` component inside the group. The default value is `4`.

<Canvas withSource="open">
	<Story
		name="Spacing"
		parameters={spacingParameters}>
		{(args) => {
			return (
				<SpacingComponent></SpacingComponent>
			)
		}}
	</Story>
</Canvas>

## Color mode

You can optionally override the color mode for this specific component. You can choose between `light` and `dark`. 

By default, the component will use the globally active color mode.

<Canvas withSource="open">
	<Story
		name="Color mode override"
		parameters={colorModeParameters}>
		{(args) => {
			return (
				<ColorModeComponent></ColorModeComponent>
			)
		}}
	</Story>
</Canvas>

## Accent color

You can optionally override the accent color for this specific component. You can choose between `blue`, `orange`, `rose`, `amber`, `indigo`, `emerald`, `red`. 

By default, the component will use the globally active accent color, defined by the theme.

<Canvas withSource="open">
	<Story
		name="Accent color override"
		parameters={accentColorParameters}>
		{(args) => {
			return (
				<AccentColorComponent></AccentColorComponent>
			)
		}}
	</Story>
</Canvas>

## Overriding color mode and accent color
Both `colorMode` and `accentColor` can be overriten at the same time

<Canvas withSource="open">
	<Story
		name="Accent color + Color mode override override"
		parameters={accentColorAndColorModeParameters}>
		{(args) => {
			return (
				<AccentColorAndColorModeComponent></AccentColorAndColorModeComponent>
			)
		}}
	</Story>
</Canvas>

## Arguments Table

<Story
	name="Arguments Table"
	argTypes={groupArgTypes}>
</Story>

<ArgsTable story="Arguments Table" />

## Style props

<Story
	name="Style props"
	argTypes={spaceArgTypes}>
	<Box marginX="-4">
		<ArgsTable story="Style props"/>
		<SpaceArgTypesComments componentName="Radio.Group"/>
	</Box>
</Story>